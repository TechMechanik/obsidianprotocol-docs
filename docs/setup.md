# Setup

This page contains quick guidance for admins to run the server locally and deploy docs.

## Local preview

1. Ensure Python 3.x is installed.
2. pip install -r requirements.txt
3. mkdocs serve

The site will be available at http://127.0.0.1:8000/ for preview.